post_install() {
    echo "========================================================================="
    echo "  Antigravity Hub installed successfully!"
    echo "  Changelog: https://antigravity.google/changelog"
    echo "========================================================================="
}

post_upgrade() {
    echo "========================================================================="
    echo "  Antigravity Hub upgraded successfully!"
    echo "  Changelog: https://antigravity.google/changelog"
    echo "========================================================================="
}

post_remove() {
    local real_user="${SUDO_USER:-$USER}"
    local real_home=$(getent passwd "${real_user}" | cut -d: -f6)
    
    if [ -d "${real_home}/.gemini/antigravity" ]; then
        echo "Removing configurations at ${real_home}/.gemini/antigravity..."
        rm -rf "${real_home}/.gemini/antigravity"
    fi
}
